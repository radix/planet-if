version: 0.2

env:
  variables:
    x: y

phases:
  install:
    commands:
      - git clone https://github.com/rubys/venus.git
  build:
    commands:
      - python venus/planet.py planet-if.conf

artifacts:
  files:
    - output/*
  discard-paths: yes
